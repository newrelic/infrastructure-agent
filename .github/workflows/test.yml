---
name: 🔍 Testing

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
    paths-ignore:
      - README.md

jobs:



  unit-test-linux:
      uses: ./.github/workflows/component_linux_unit_test.yml
      secrets:
        gh_token: ${{secrets.GITHUB_TOKEN}}


