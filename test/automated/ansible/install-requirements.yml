---

- hosts: testing_hosts
  become: false
  gather_facts: no

  tasks:
    - name: install python
      include_role:
        name: install-python
    - name: install libcap
      include_role:
        name: install-libcap
    - name: install gpg
      include_role:
        name: install-gpg
