---
- name: 'install python on systems managed by zypper '
  zypper:
    name: python3
    state: present
    update_cache: yes
  when: inventory_hostname is search(item)
  loop: "{{ instances_without_python_zyp }}"
...