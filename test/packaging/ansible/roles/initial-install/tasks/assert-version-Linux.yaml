---

- name: Assert expected version
  shell: "newrelic-infra --version | grep {{ lookup ('env', 'INITIAL_AGENT_VERSION') }} || (>&2 newrelic-infra --version && exit 1)"
  register: check

- name: Stdout from version grep
  debug:
    msg: "{{ check.stdout }}"

...
