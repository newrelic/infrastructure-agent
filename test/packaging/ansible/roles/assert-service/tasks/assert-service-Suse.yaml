---

# Either user a newer version of ansible or ignore_errors is needed https://github.com/ansible/ansible-modules-core/pull/1846
- name: ensure newrelic-infra is up and running, Suse
  service:
    name: newrelic-infra
    state: started
    enabled: yes
    use: sysvinit
    runlevels: "3"
  ignore_errors: yes

...
