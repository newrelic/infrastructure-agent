---

- include: "config-{{ ansible_system }}.yaml"
- include: "package-{{ ansible_os_family }}.yaml"
- include: "assert-service-{{ ansible_os_family }}.yaml"
- include: "assert-version-{{ ansible_system }}.yaml"
- include: "check-{{ ansible_system }}.yaml"

...
