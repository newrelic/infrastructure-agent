collections:
  # TEMPORARY - validating NR-598649 nri-docker relocation fix before it merges. Revert to the
  # default branch (drop `version:`) before merging this PR.
  - name: git+https://github.com/newrelic-experimental/caos-ansible-roles.git#/caos.ansible_roles/
    type: git
    version: NR-598649-relocate-nri-docker

roles:
  - name: geerlingguy.pip
