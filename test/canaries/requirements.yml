collections:
  # TODO: temporary pin for A2Q canary log-forwarding validation, revert to unpinned once
  # https://github.com/newrelic-experimental/caos-ansible-roles/tree/feature/a2q-canaries-fluent-bit-logs is merged
  - name: git+https://github.com/newrelic-experimental/caos-ansible-roles.git#/caos.ansible_roles/
    version: feature/a2q-canaries-fluent-bit-logs
    type: git

roles:
  - name: geerlingguy.pip
