---
- src: "newrelic-infra-amd64.{version}.zip"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/amd64/{src}"

- src: "newrelic-infra-386.{version}.zip"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/386/{src}"


#########################
  # UPLOAD CHECKSUMS #
#########################
- src: "newrelic-infra-amd64.{version}.zip.sum"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/amd64/{src}"

- src: "newrelic-infra-386.{version}.zip.sum"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/386/{src}"
