---
- src: "newrelic-infra-amd64.{version}.zip"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/amd64/{src}"


#########################
  # UPLOAD SIGNATURE #
#########################

- src: "newrelic-infra-amd64.{version}.zip.cat"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/amd64/{src}"


#########################
  # UPLOAD CHECKSUMS #
#########################

- src: "newrelic-infra-amd64.{version}.zip.sum"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/amd64/{src}"

